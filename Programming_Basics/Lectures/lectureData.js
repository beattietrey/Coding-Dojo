var myName = "Trey";
console.log(myName);

// Booleans are true or false (Binary, so 0 is false, anything else is true.)

var isSnowing = false

if(isSnowing == true && isSnowing != false) {
    console.log("Perfect")
}
else {
    console.log("Aww shucks.")
}

//Operators
//  + == = - * / && means "and" || means "or" != += means "shorthand for adding strings to strings"

var lastName = " Beattie"

myName = myName + lastName
