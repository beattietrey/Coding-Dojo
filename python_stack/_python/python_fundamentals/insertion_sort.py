def insertion_sort(li):
    for i in range(0,len(li)-1,1):
        if li[i] > li[i+1]:
        # starting at the beginning, check to see where the number fits, by finding the numbers where it is smaller than the right and greater than the left
            for j in range(0,i,1):
                
    return li



print(insertion_sort([9,8,7,6,5,4,3,2,1]))